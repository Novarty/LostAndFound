<nav class="navbar navbar-default" role="navigation" style="border-radius: 0px;">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
      <a class="navbar-brand" href="#">Lost and Found</a>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
      <% if current_page?(root_path) %>
        <li class="active"><a href="..">Home</a></li>
        <% else %>
        <li><a href="..">Home</a></li>
        <% end %>
        <li><a href="../items/new">I'm founder</a></li>
      </ul>
      <%= form_tag items_path, method: 'get', class: 'navbar-form navbar-left' do %>
      <%= text_field_tag :search, params[:tag], placeholder: 'Search', class: "form-control", required: true %>
      <%= submit_tag "Search", class: 'btn btn-default'%>
      <% end %>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.name %> <%= current_user.surname %> <b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><%=link_to "Profile", user_path(current_user)%></li>
              <li><a href="#">My items</a></li>
              <li><a href="#">Settings</a></li>
              <li class="divider"></li>
              <li><%=link_to "Sign out", destroy_user_session_path, method:"delete"%></li>
            </ul>
          </li>
        <% else %>
          <% if current_page?(new_user_session_path) %>
            <li class="active"><%=link_to 'Sign in', new_user_session_path %></li>
          <% else %>
            <li><%=link_to 'Sign in', new_user_session_path%></li>
            <% end %>
          <% end %>
      </ul>
    </div>
  </div>
</nav>
